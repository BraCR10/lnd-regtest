# Bitcoin Core RPC credentials (required — must match your bitcoin.conf)
BITCOIND_RPC_USER=your_rpc_user
BITCOIND_RPC_PASS=your_rpc_password

# LND wallet password (optional — if set, skips the interactive prompt)
# WALLET_PASS=your_wallet_password

# Bitcoin Core connection (optional — defaults shown)
# BITCOIND_HOST=127.0.0.1
# BITCOIND_RPC_PORT=18443

# Miner wallet name in bitcoind (optional)
# MINER_WALLET=miner

# LND Docker image (optional)
# LND_IMAGE=lightninglabs/lnd:v0.20.1-beta

# Funding amounts (optional)
# FUND_LND1_BTC=8
# FUND_LND2_BTC=3

# Channel size and rebalance amount in satoshis (optional)
# CHANNEL_SATS=500000000
# REBALANCE_SATS=250000000

# RTL (Ride The Lightning) web UI (optional — defaults shown)
# RTL_IMAGE=shahanafarooqui/rtl:latest
# RTL_PORT=3000
# RTL_PASSWORD=your_rtl_password    # defaults to WALLET_PASS if not set

# Mostro P2P exchange (optional — defaults shown)
# MOSTRO_IMAGE=mostrop2p/mostro:latest
# MOSTRO_RELAY_IMAGE=scsibug/nostr-rs-relay
# MOSTRO_RELAY_PORT=7000
# MOSTRO_NSEC_PRIVKEY=nsec1...      # if set, skips key prompt/generation
# MOSTRO_RELAYS=wss://nos.lol,wss://relay.mostro.network   # comma-separated, added alongside local relay

# Lightning Address via satdress (optional — requires a domain with DNS pointing to this server)
# LNURL_DOMAIN=yourdomain.com         # enables step 9 (nginx + satdress + certbot)
# LNURL_USERNAMES=admin               # comma-separated: admin,user2 → admin@yourdomain.com, user2@yourdomain.com
# SATDRESS_PORT=17422
