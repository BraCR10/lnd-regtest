# Bitcoin Core RPC credentials (required — must match your bitcoin.conf)
BITCOIND_RPC_USER=your_rpc_user
BITCOIND_RPC_PASS=your_rpc_password

# LND wallet password (optional — if set, skips the interactive prompt)
# WALLET_PASS=your_wallet_password

# Bitcoin Core connection (optional — defaults shown)
# BITCOIND_HOST=127.0.0.1
# BITCOIND_RPC_PORT=18443

# Miner wallet name in bitcoind (optional)
# MINER_WALLET=miner

# LND Docker image (optional)
# LND_IMAGE=lightninglabs/lnd:v0.20.1-beta

# Funding amounts (optional)
# FUND_LND1_BTC=8
# FUND_LND2_BTC=3

# Channel size and rebalance amount in satoshis (optional)
# CHANNEL_SATS=500000000
# REBALANCE_SATS=250000000
